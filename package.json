{
  "name": "cloudLib",
  "version": "0.0.1",
  "devDependencies": {
    "bower": "*",
    "karma": "*",
    "phantomjs": "*"
  },
  "scripts": {
     "test": "bower install && ./node_modules/.bin/karma start --single-run --browsers PhantomJS"
  }
}